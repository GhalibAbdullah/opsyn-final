services:
  - type: web
    name: activepieces-server
    runtime: node
    plan: free
    rootDir: packages/server
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
